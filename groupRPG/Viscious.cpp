#include <string>
#include "Viscious.h"
#include <iostream>
#include <ctime>

using namespace std;

void Viscious::setSubEnemy()                     // will need to return an object
{
	int useElement = randomize(0, 100);

	//(useElement % 2 == 0 ? subEnemiesArray[0] : subEnemiesArray[1]);
}
